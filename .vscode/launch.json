{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Raylib Game (gdb)",
            "type": "cppdbg",
            "request": "launch",
            // Caminho para o executável.
            "program": "${workspaceFolder}/mechaGame",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Build Raylib Game (WSL)"
        }
    ]
}
