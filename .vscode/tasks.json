{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project with Make",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "make run",
            "args": [],
            
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}

